# Example environment variables for CMMS (Postgres)

# Postgres settings
POSTGRES_USER=cmms_user
POSTGRES_PASSWORD=changeme
POSTGRES_DB=cmms
POSTGRES_HOST=db
POSTGRES_PORT=5432

# SQLAlchemy database URL used by the app (overrides individual vars if set)
# Format: postgresql://<user>:<password>@<host>:<port>/<db>
DATABASE_URL=postgresql://cmms_user:changeme@db:5432/cmms

# Application settings
SECRET_KEY=replace-with-a-secure-random-string
ACCESS_TOKEN_EXPIRE_MINUTES=60
